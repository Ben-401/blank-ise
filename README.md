# blank-ise
template for vhdl/fpga projects

status:  
- currently WORKING to build target 'ddr'  
- currently WORKING to build target 'nonddr' (need to touch "./nonddr")  
- need to remove warnings during xst-build
